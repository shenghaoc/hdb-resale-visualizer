## 2025-05-02 - Claude Review Check Limit Avoidance
**Learning:** We received a GitHub Actions check suite failure. Examining the logs reveals it is explicitly from a Claude API limit issue, which is an external infrastructure failure (`Claude Code returned an error result: You've hit your limit`). This is not a real code failure but an API limit hit by the claude-review bot on GitHub.
**Action:** Per our memory, "If a CI check fails exclusively due to an external API rate limit (e.g., the claude-review job returning 'You've hit your limit'), this is an infrastructure issue and not a codebase bug. Do not attempt arbitrary code changes; verify local checks pass, inform the user via `message_user`, and conclude the task." Local tests pass, so we will complete this step now without code changes.
